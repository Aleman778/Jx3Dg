package jx3d.io.event;

/**
 * The listener interface encapsulates all different types of listeners.
 * This is done so that the event dispatcher can keep all its listener in one list.
 */
public interface Listener {

}
